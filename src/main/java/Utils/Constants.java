package Utils;

public class Constants {
    // Usuarios
    public static final String SELECT_USUARIOS_NOMBRE_CONTRASENA = "SELECT nombre, password FROM usuarios WHERE nombre = ? AND password = ?";


}
